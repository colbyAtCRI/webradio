# webradio
websocket server for SDRplay radios based on v2.13 for RSPdup and RSPII radios.

As it stands only one radio may be served per process. This is a limitation
of the SDRplay API which is v2.13. The libwebsockets version is 3.0.1 or so
with the corresponding openssl both of which were compiled locally.  
